// Default (dev) runtime config. In production the nginx entrypoint overwrites
// this file with environment-specific values. Keep this as an empty default so
// dev falls back to Vite's import.meta.env.
window.__SPELLNOOK_CONFIG__ = window.__SPELLNOOK_CONFIG__ || {};
